
import { ComingSoon } from '@/components/ComingSoon';

interface MilestoneTrackerProps {
  childId: string;
}

const MilestoneTracker = ({ childId }: MilestoneTrackerProps) => {
  return <ComingSoon feature="Milestone Tracking" />;
};

export default MilestoneTracker;
